[project]
name = "pandas-cleaning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Re7ab", email = "re7ab.su95@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pandas-cleaning = "pandas_cleaning:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
